# Dependencies Ledger

Companion to [Design.md](Design.md) — satisfies [DevelopmentGuidelines.md](DevelopmentGuidelines.md)'s requirement to track, per dependency: **version, release date, source, and what security validation has been performed**.

**Status: not yet pinned.** No dependency has been installed yet (project is still in the design phase — see [Design_Notes.md](Design_Notes.md)). This ledger currently records the *architectural choice and vetting rationale* for each candidate; the **Version** and **Release Date** columns get filled in at the point each dependency is actually added via `uv add` (Phase 1, [Design.md §11](Design.md#11-phase-1--detailed-breakdown)) and re-verified whenever it's upgraded.

**Ongoing security validation process:**
- New dependency, at adoption time: check maintenance activity (recent releases/commits), check for known CVEs (PyPI advisory DB / OSV), check adoption/provenance signals (org-backed vs. single maintainer, download counts), check license compatibility.
- Existing dependencies, continuously: `pip-audit` and `bandit` run in CI on every push/PR ([Design.md §7.4](Design.md#74-development-tooling)); GitHub **Dependabot** (version updates + security alerts) should be enabled on the repo once it's on GitHub, since CI already runs there (Decision #13) — cheap addition, flagged here as a recommendation rather than a blocking decision.
- Each row below gets a **Last Reviewed** date when its vetting notes are written or revisited.

---

## 1. Core Bus Library (`jetcore`)

| Dependency | Source | Version | Release Date | Purpose | Security Vetting Notes | Last Reviewed |
|---|---|---|---|---|---|---|
| `nats-py` | [PyPI](https://pypi.org/project/nats-py/) / [GitHub](https://github.com/nats-io/nats.py) | **2.15.0** (resolved via `uv add "nats-py[nkeys]"`) | Official async NATS/JetStream client; also provides the `[nkeys]` extra used for signing. Added in Step B3 to get `nkeys`; the client itself is exercised starting [registry.py](libs/jetcore/jetcore/registry.py) (§11 Step B5). | Official `nats-io` org project (backed by Synadia); same trust tier as the NATS server itself. Ships a `py.typed` marker (unlike `nkeys`/`pyrage`), but not every method is annotated — `KeyWatcher.updates` needed a narrow `type: ignore[no-untyped-call]`. **KV API has real surprises, confirmed by testing, not guessable from the API shape**: `KeyValue.keys(filters=...)` looks like a NATS wildcard filter but is actually a client-side Python substring check on already-fetched keys — a literal `*`/`>` never matches anything there. `KeyValue.watch(keys=...)`, by contrast, genuinely does use its argument as a NATS subject with real server-side filtering. See §11 Step B5 for the full list (5 findings). | 2026-08-24 |
| `nkeys` | [PyPI](https://pypi.org/project/nkeys/) / [GitHub](https://github.com/nats-io/nkeys.py) | **0.2.1** (resolved via `uv add`) | Ed25519 keypair generation/signing for both NATS connection auth and event signing (§4.1). Installed via `nats-py[nkeys]`. Used in [crypto.py](libs/jetcore/jetcore/crypto.py) (§11 Step B3). | Official `nats-io` org project. Implements NATS's own key format (not custom crypto). **Confirmed real bug** (not assumed): `KeyPair.verify()` only works with the private key already loaded — there's no working way to verify a signature from just a public key, defeating the actual point of asymmetric verification. Read the library's source to confirm, then worked around in `crypto.py` with a from-scratch, tested decode of the public-key encoding feeding `nacl.signing.VerifyKey` directly. Worth flagging upstream; not filed as of this writing. | 2026-08-24 |
| `pydantic` (v2) | [PyPI](https://pypi.org/project/pydantic/) | **2.13.4** (resolved via `uv sync`) | Event envelope ([envelope.py](libs/jetcore/jetcore/envelope.py), §11 Step B2), config, and adapter payload schema modeling/validation. | Extremely widely adopted (FastAPI's foundation); large maintainer team, active CVE monitoring history. Verified: 10 tests pass including exact wire-shape fidelity to §5; note the mypy plugin (`pydantic.mypy`, now enabled in root `pyproject.toml`) doesn't understand `populate_by_name=True` — confirmed by testing, see Decision-adjacent note at Step B2. | 2026-08-24 |
| `pydantic-settings` | [PyPI](https://pypi.org/project/pydantic-settings/) | **2.15.0** (resolved via `uv add`) | Env-var/`.env`-driven adapter configuration loading — [config.py](libs/jetcore/jetcore/config.py) (§11 Step B4). | Maintained by the Pydantic org itself; same trust tier as `pydantic`. **Confirmed real behavior** (not assumed): it attempts its own JSON-decode of list-typed env values *before* any `field_validator` runs, at the settings-source layer — a plain comma-separated value fails there outright. Worked around with `Annotated[list[str], NoDecode]`, deferring to a custom validator instead; see §11 Step B4. | 2026-08-24 |
| `python-dotenv` | [PyPI](https://pypi.org/project/python-dotenv/) | **1.2.3** (transitive, via `pydantic-settings`'s `env_file` support) | Powers `AdapterSettings`'s `.env` file loading. | Long-established, widely used; not directly declared, pulled in by `pydantic-settings` itself. | 2026-08-24 |
| `pyrage` | [PyPI](https://pypi.org/project/pyrage/) / [background](https://blog.yossarian.net/2022/07/25/age-encryption-in-python-with-pyrage) | **1.4.0** (resolved via `uv add`) | Python bindings (via PyO3) to `rage`, the Rust implementation of the [age](https://age-encryption.org) format — provides the multi-recipient hybrid encryption in §4.3. Used in [crypto.py](libs/jetcore/jetcore/crypto.py) (§11 Step B3). | The cryptography itself is implemented in vetted Rust (`rage`/`age` core), not reimplemented in Python — bindings only. PyPI releases use Trusted Publishing (supply-chain provenance signal). Smaller install base than mainstream crypto libs; **recommendation:** re-check for an independent security review of `rage` before production use. **Adoption confirmed working as designed**, with one real schema implication: age bundles all per-recipient key-wrapping inside its ciphertext (verified by inspecting real encrypt output), which is why `envelope.py`'s `recipients` field changed to a flat key-string list — see §11 Step B3. `pip-audit`: no known vulnerabilities. | 2026-08-24 |
| `PyNaCl` | [PyPI](https://pypi.org/project/PyNaCl/) | **1.6.2** (pulled in transitively by `nkeys` — not directly declared) | libsodium bindings. Originally the documented fallback if `pyrage` integration proved impractical (§7.2) — turned out to be in the dependency tree anyway, just via `nkeys` rather than as jetcore's own choice. Now doing real work in [crypto.py](libs/jetcore/jetcore/crypto.py): `nacl.signing.VerifyKey` is used directly to work around the `nkeys.py` verification bug noted above. | Long-established, widely audited (wraps libsodium). `pip-audit`: no known vulnerabilities. | 2026-08-24 |

## 2. Adapter-Specific

| Dependency | Source | Version | Release Date | Purpose | Security Vetting Notes | Last Reviewed |
|---|---|---|---|---|---|---|
| `fastapi` | [PyPI](https://pypi.org/project/fastapi/) | **0.141.1** (resolved via `uv add --package webhook-listener` at the time — that package is now named `jetcore-webhook-listener`, see Decision #28; also depended on directly by `jetcore-rest-api-service` from Step I2) | *resolved 2026-08-27* | ASGI HTTP surface for both the Webhook Listener ([app.py](adapters/webhook_listener/webhook_listener/app.py), Step D3) and the REST API Service ([app.py](adapters/rest_api_service/rest_api_service/app.py), Step I5) — the second adapter to also combine it with a background `asyncio` consumer loop started from its own `lifespan` (the reply watcher, Step I4), not just a request/response surface. | Extremely widely adopted; large, active maintainer community. Verified: `FastAPI`'s `lifespan` context manager only runs inside `TestClient`'s `with` block — a bare `TestClient(app)` used outside one silently never triggers it, confirmed by testing (not documented anywhere obvious), which left `app.state.bus_client` unset until this was caught. | 2026-08-29 |
| `uvicorn` | [PyPI](https://pypi.org/project/uvicorn/) | *pin at adoption* (also depended on directly by `jetcore-rest-api-service` from Step I2) | *pin at adoption* | ASGI server running the FastAPI apps above. | Standard companion to FastAPI; widely adopted. | 2026-08-29 |
| `httpx` | [PyPI](https://pypi.org/project/httpx/) | **0.28.1** (resolved via `uv add --package jetcore-webhook-sender`, Design.md §13 Step G3; also depended on directly by `jetcore-http-adapter` from Step H1) | *resolved 2026-08-28* | Async outbound HTTP for both the Webhook Sender ([relay_handler.py](adapters/webhook_sender/webhook_sender/relay_handler.py)) and the HTTP Adapter ([trigger_handler.py](adapters/http_adapter/http_adapter/trigger_handler.py)) — a distinct concern from `httpx2` below. | Maintained by Encode (same org as `starlette`/`uvicorn`); widely adopted. Verified against a real local `http.server`-based test receiver in both adapters (Steps G5/H5), not mocked — relay-on-receipt/trigger-to-call mapping, secret/auth headers, and both an HTTP-error and a connection-refused failure mode all confirmed (Decision #12: Webhook Sender always acks best-effort; HTTP Adapter acks any real response but naks a genuine connection failure for redelivery — a deliberate divergence, see Step H3). | 2026-08-29 |
| `httpx2` | [PyPI](https://pypi.org/project/httpx2/) | **2.12.0** (resolved via `uv add --dev`) | *resolved 2026-08-27* | `starlette`'s `TestClient` (used by [test_webhook_listener_app.py](adapters/webhook_listener/tests/test_webhook_listener_app.py), Step D3) requires it — a plain `httpx` install works too but logs a `StarletteDeprecationWarning` telling you to install this instead (confirmed by testing, not assumed); a genuinely separate PyPI package from `httpx`, not a version bump, presumably to let both coexist during `starlette`'s migration. Shared dev-group dependency — every future FastAPI-based adapter's tests need the same thing. | New package (backs `httpx`'s own ecosystem, same maintainers as `starlette`/`fastapi` depend on) — low risk, but young; `pip-audit`: no known vulnerabilities. | 2026-08-27 |
| `tenacity` | [PyPI](https://pypi.org/project/tenacity/) | *pin at adoption* | *pin at adoption* | Retry/backoff for Database Adapter writes (not the Webhook Sender, which is best-effort per Decision #12). | Long-established, widely adopted retry library. | 2026-08-23 |
| `SQLAlchemy` (2.0, async) | [PyPI](https://pypi.org/project/SQLAlchemy/) | *pin at adoption* | *pin at adoption* | ORM/core for the Database Adapter's write path. | De facto standard Python SQL toolkit; long track record. | 2026-08-23 |
| `asyncmy` | [PyPI](https://pypi.org/project/asyncmy/) | *pin at adoption* | *pin at adoption* | Async MySQL driver used under SQLAlchemy. | Smaller community than `mysqlclient`/`PyMySQL`; **recommendation:** re-check maintenance activity at adoption time, since MySQL async driver options churn. | 2026-08-23 |
| `mysql-replication` (import name `pymysqlreplication`, the "`python-mysql-replication`" project) | [PyPI](https://pypi.org/project/mysql-replication/) / [GitHub](https://github.com/julien-duponchelle/python-mysql-replication) | *pin at adoption* | last seen updated Oct 2024 | Pure-Python MySQL binlog protocol client (built on PyMySQL) — powers the Database Adapter's CDC read path (Decision #15). | No independent third-party security audit found publicly. Single-maintainer-led project, though it's cited as used in production at "medium internet corporations" per its own docs. **Mitigations planned:** run it against a MySQL account restricted to `REPLICATION SLAVE`/`REPLICATION CLIENT` grants only (least privilege — no read/write access to actual table data needed for binlog tailing); treat parsed row-events as untrusted input and validate/schema-check before publishing to the bus. | 2026-08-23 |
| `aiofiles` | [PyPI](https://pypi.org/project/aiofiles/) | **25.1.0** (resolved via `uv add --package file-storage-adapter`) | Async file I/O backing the File Storage Adapter's write-path command handler — [write_handler.py](adapters/file_storage_adapter/file_storage_adapter/write_handler.py) (Design.md §12 Step C4). | Small, focused, widely used utility library. Verified: real async writes to a real filesystem in the C4 integration tests, not mocked. | 2026-08-25 |
| `watchfiles` | [PyPI](https://pypi.org/project/watchfiles/) | **1.2.0** (resolved via `uv add --package file-storage-adapter`) | Detects files created by outside processes so the File Storage Adapter can emit `FileCreateCompleted` without a preceding command (Decision #19) — [create_watch.py](adapters/file_storage_adapter/file_storage_adapter/create_watch.py) (Design.md §12 Step C5). | Rust-backed, async-native; maintained by Samuel Colvin (also the `pydantic` author) — same trust tier as the rest of the Pydantic-ecosystem picks already in this ledger. **Confirmed real behavior, not assumed**: default `debounce=1600ms` (from `awatch`'s own signature) — an "added" event fires on file creation, not once a slow write finishes, and gives no "settled" guarantee; Step C5 built its own settle-poll on top rather than trusting the raw event. Also pulled in `anyio` as a transitive dependency. | 2026-08-25 |
| `uuid6` | [PyPI](https://pypi.org/project/uuid6/) | **2025.0.1** (resolved via `uv sync`) | `uuid7()` for event IDs — stdlib `uuid` doesn't gain native support until Python 3.14 (project targets 3.12, Decision #8). Wrapped by `new_event_id()` in [envelope.py](libs/jetcore/jetcore/envelope.py) (§11 Step B2). | Small, single-purpose implementation of a published RFC (RFC 9562); low risk surface. Verified: generates genuinely unique, correctly time-ordered ids (tested). | 2026-08-24 |
| `structlog` | [PyPI](https://pypi.org/project/structlog/) | *pin at adoption* | *pin at adoption* | Structured JSON logging correlated by `eventId`/`correlationId`. | Long-established, widely adopted. | 2026-08-23 |
| `typer` | [PyPI](https://pypi.org/project/typer/) | *pin at adoption* | *pin at adoption* | CLI tooling — key generation, registry admin, `tools/` scripts (§11). | Maintained by the FastAPI author; widely adopted. | 2026-08-23 |

## 3. Development & QA Tooling

| Dependency | Source | Version | Release Date | Purpose | Security Vetting Notes | Last Reviewed |
|---|---|---|---|---|---|---|
| `uv` | [PyPI](https://pypi.org/project/uv/) / [docs](https://docs.astral.sh/uv/) | **0.12.5** (released 2026-08-14), installed via the official user-space installer, no sudo | Packaging/dependency management, workspace resolution (Decision #16). | Backed by Astral (same org as `ruff`); rapidly growing adoption as the modern default. Verified against a real workspace — see the `--all-packages` gotcha noted in [README.md](README.md). | 2026-08-24 |
| `pytest`, `pytest-asyncio`, `pytest-cov` | [PyPI](https://pypi.org/project/pytest/) | **9.1.1 / 1.4.0 / 7.1.0** (resolved via `uv sync`, `uv.lock`) | Test framework + async support + coverage. | De facto standard Python test tooling. Verified: `libs/jetcore`'s scaffold test passes. | 2026-08-24 |
| `testcontainers-python` | [PyPI](https://pypi.org/project/testcontainers/) | *pin at adoption* (not yet added — needed starting §10 Phase 4) | Spins up real NATS+JetStream and MySQL containers for integration tests (§10 Phase 4). | Widely adopted across language ecosystems (originated in Java); active Python port. | 2026-08-23 |
| `ruff` | [PyPI](https://pypi.org/project/ruff/) | **0.16.4** (resolved via `uv sync`) | Lint + format. | Backed by Astral; has effectively become the standard, replacing flake8/isort/black for new projects. Verified: `ruff check` and `ruff format` both pass clean on the scaffold. | 2026-08-24 |
| `mypy` | [PyPI](https://pypi.org/project/mypy/) | **2.3.1** (resolved via `uv sync`) | Static type checking. | Long-established, reference type checker for Python. Verified — and note: **hard-errors on a missing path argument** (unlike pytest, which tolerates a missing `testpaths` entry), which broke the initial pre-commit config until `adapters/` was dropped from its args (added back once Phase 2/§12 Step C1 actually created it). | 2026-08-24 |
| `bandit` | [PyPI](https://pypi.org/project/bandit/) | **1.9.4** (resolved via `uv sync`) | Static security analysis of our own code. | PyCQA project; long track record as the standard Python SAST tool. Verified clean on the scaffold. | 2026-08-24 |
| `pip-audit` | [PyPI](https://pypi.org/project/pip-audit/) | **2.10.1** (resolved via `uv sync`) | Dependency vulnerability scanning (checks this ledger's contents against known CVEs on every CI run). | Maintained by the PyPA (official Python Packaging Authority) org. Verified: no known vulnerabilities found in the current dependency set. | 2026-08-24 |
| `pre-commit` | [PyPI](https://pypi.org/project/pre-commit/) | **4.6.2** (resolved via `uv sync`) | Git hook orchestration tying the above together locally. | Long-established, widely adopted. Verified end-to-end (all 4 hooks pass); note it only checks git-tracked files, not arbitrary untracked ones — see README. | 2026-08-24 |
| `jsonschema` | [PyPI](https://pypi.org/project/jsonschema/) | **4.26.0** (resolved via `uv add --dev`) | Validates `schemas/*.json` are well-formed JSON Schema, and that example payloads conform to them — [test_schemas.py](adapters/file_storage_adapter/tests/test_schemas.py) (Design.md §12 Step C3). Shared dev-group dependency, not a runtime dependency of any adapter (v1 doesn't validate against schemas at runtime). | Reference implementation, maintained under the `python-jsonschema` GitHub org; the de facto standard Python JSON Schema validator, widely adopted. | 2026-08-25 |
| `types-jsonschema` | [PyPI](https://pypi.org/project/types-jsonschema/) | **4.26.0.20260518** (resolved via `uv add --dev`) | Type stubs for `jsonschema` — `mypy --strict` reported `import-untyped` without them (confirmed by testing). | Part of the `python/typeshed` project's third-party stub distribution (`types-*` namespace); same trust tier as typeshed itself. | 2026-08-25 |
| `types-aiofiles` | [PyPI](https://pypi.org/project/types-aiofiles/) | **25.1.0.20260518** (resolved via `uv add --dev`) | Type stubs for `aiofiles` — same `mypy --strict` `import-untyped` gap as `jsonschema` above, same fix. | `python/typeshed` third-party stub distribution. | 2026-08-25 |

## 4. Infrastructure Services (non-Python)

| Dependency | Source | Version | Purpose | Security Vetting Notes | Last Reviewed |
|---|---|---|---|---|---|
| NATS Server (JetStream) | [nats.io](https://nats.io) / [GitHub](https://github.com/nats-io/nats-server/releases) | **2.14.5** (released 2026-08-12), via Docker image `nats:2.14.5` — no native install | The message bus itself. | Official, CNCF-hosted project; core to the whole design (§3–§6). Pinned in [infra/nats/README.md](infra/nats/README.md) (Phase 1, Step A1). | 2026-08-23 |
| `nats-box` (bundles `nsc` + `nats` CLI + `nkeys` + `nats-top`) | [GitHub](https://github.com/nats-io/nats-box) / [Docker Hub](https://hub.docker.com/r/natsio/nats-box/tags) | **0.19.7-nonroot**, via Docker image `natsio/nats-box:0.19.7-nonroot` — no native install | Issues Operator/Account/User JWTs for decentralized auth (§4.2, §11 Track A3), and provides the `nats` CLI for manual smoke testing (§11 Step A7). Persistent `nsc` state is a volume mount, not host-installed state. | Official `nats-io` utilities image. `-nonroot` variant used deliberately (least privilege). Bundled tool versions aren't independently pinned by us — see [infra/nats/README.md](infra/nats/README.md) for how to check them. | 2026-08-23 |
| MySQL | [mysql.com](https://www.mysql.com/) | *pin at adoption* | Database Adapter's backing store; binlog source for CDC. | Industry-standard RDBMS. | 2026-08-23 |
| Docker / Docker Compose | [docker.com](https://www.docker.com/) | *pin at adoption* | Local deployment/dev environment (§10 Phase 5). | Industry-standard containerization. | 2026-08-23 |
| `yq` (mikefarah) | [GitHub](https://github.com/mikefarah/yq) / [Docker Hub](https://hub.docker.com/r/mikefarah/yq/tags) | **4.52.4**, via Docker image `mikefarah/yq:4.52.4` — no native install | Parses [infra/nats/adapter_identities.yaml](infra/nats/adapter_identities.yaml) to JSON, and does all JSON field extraction, in [bootstrap_auth.sh](infra/nats/bootstrap_auth.sh) (§11 Step A3). | Widely adopted Go-based YAML processor (distinct from the Python `yq`/jq wrapper of the same name — this is mikefarah's). Verified against a real run — no issues found. Originally paired with `jq` for JSON extraction; `jq` turned out not to be present on the host, so the script was rewritten to do everything through `yq` alone rather than add another native dependency. | 2026-08-23 |

---

*Update this ledger whenever a dependency is added, upgraded, or re-reviewed — don't let it drift out of sync with the actual `uv.lock`.*
