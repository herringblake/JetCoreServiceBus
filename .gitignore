# --- NATS auth material (Design.md §11 Track A parameters: never committed) ---
# nsc's Operator/Account/User store (nkeys + JWTs) and everything derived
# from it (generated .creds files, the memory-resolver config, the operator
# JWT) are regenerated locally by infra/nats/bootstrap_auth.sh, not committed.
infra/nats/operator/
infra/nats/resolver.conf
infra/nats/operator.jwt

# --- Python (uv workspace — Design.md §7.5, Track B) ---
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# --- Adapter runtime data (Design.md §12 Step E1) ---
# File Storage Adapter's watch_dir bind mount — real file content written
# by/through the running stack, not part of the repo.
infra/files/

# --- General ---
.env
.DS_Store
