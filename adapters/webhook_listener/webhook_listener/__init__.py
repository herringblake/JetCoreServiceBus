"""webhook_listener — inbound HTTP webhook receiver (Design.md §8, §12
Track D).

Step D1 scaffold. Real modules (settings, HTTP endpoint, entrypoint) land
in Steps D2-D4 — this package is intentionally empty of application logic
until then.
"""

__version__ = "0.1.0"
